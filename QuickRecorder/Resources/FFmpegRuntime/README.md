# FFmpeg Runtime Placeholder

Generated FFmpeg binaries are intentionally excluded from Git. Use
`Tools/Archive/package_ffmpeg_runtime.py` to create a local runtime package and
review all generated license notices before distribution.

Public releases must provide a reproducible runtime artifact for every
supported architecture; a developer's local Homebrew installation is not a
release supply chain.
